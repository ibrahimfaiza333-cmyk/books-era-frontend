"use client";
/** Centered spinner for tables / inline sections. */
const InlineSpinner = () => (
    <div className="w-8 h-8 border-4 border-orange-500 border-t-transparent rounded-full animate-spin mx-auto" />
)

export default InlineSpinner
